global:
  scrape_interval: 5s
rule_files:
  - /etc/prometheus/rules/*.rules.yml
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
scrape_configs:
  - job_name: runtimed
    metrics_path: /metrics
    static_configs:
      - targets: ["runtimed:8080"]
